# panda_harvester_workers

Use when the user asks about Harvester workers, worker states, or recent worker counts by site.

Common parameters:
- site: compute site filter
- hours: look back this many hours

Output type:
JSON object with live/recent Harvester worker counts by state and site.

Presentation:
Show worker counts in a table grouped by site and worker state.
